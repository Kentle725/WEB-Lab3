# Лабораторная работа №3: Используем форму из ЛР-2, но теперь обработка данных будет выполняться на сервере через PHP. Данные сохраняем в сессии, а затем в файл.

## 👩‍💻 Автор
ФИО: Баграмов Владимир Андреевич  
Группа: ПМИ2 ИП1 ПГ2

---

## 📌 Описание задания
Научиться обрабатывать данные формы на стороне сервера с помощью PHP.
Сохранять данные формы в сессии.
Сохранять данные в файл и выводить их обратно на странице.
Научиться выводить все сохранённые данные в отдельной странице.
Научиться изменять существующую форму, чтобы отключить JS-обработку (кроме alert) и использовать PHP.

Результат доступен по адресам: 
[http://localhost:8080/index.php](http://localhost:8080/index.php) - Вывод данных из сессии
[http://localhost:8080/form.html](http://localhost:8080/form.html) - Форма
[http://localhost:8080/view.php](http://localhost:8080/view.php) - Все сохранённые данные

---

## ⚙️ Как запустить проект

1. Клонировать репозиторий:
   ```bash
   git clone <ссылка на репозиторий>
   cd WEB-Lab3
Запустить контейнеры:
```bash
docker-compose up -d --build
```
Открыть в браузере:
```http://localhost:8080/form.html```
📂 Содержимое проекта

```docker-compose.yml``` — описание сервиса Nginx и PHP-FPM

```nginx/default.conf``` — конфигурационный файл сервера Nginx

```www/index.php``` — PHP-файл с выводом данных из сессии

```www/form.html``` — страница с формой

```www/view.php``` — PHP-файл со всеми сохранёнными данными

📸 Скриншоты работы

✅ Результат
Сервер в Docker успешно запущен, Nginx отдаёт PHP-файл с данными из сессии, HTML-страницу с формой и PHP-файл со всеми сохранёнными данными